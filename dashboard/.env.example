# Base URL of the main site's deployment, where /api lives (e.g. https://devstudios.com)
VITE_API_BASE_URL=
# Set to "true" to run the dashboard against fake in-memory data with no backend
VITE_DEMO_MODE=false
